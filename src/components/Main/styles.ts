import styled from "styled-components";
import vari from "../../styles/var";

export const Block = styled.div` 
    width: 80%;
    height: 500px;
    margin: 20px auto;
    border-radius: 16px;
    box-shadow: ${vari.sombra};
`

export const Titulo = styled.h2`
    color: ${vari.deepBlue};
    font-weight: 700;
    font-size: 40px;
    margin: 0 auto;
    text-align: center;
    text-shadow: ${vari.sombra};

`
